Parameters for the (Thumbs) thumbnail matching pipeline stage.

PXBRO=1          <default 2>      "Max Leg poly order for bkg removal"
PXDOG=N          <default N>      "Y=Apply DoG filter"
PXDOG_R1=3       <default 3>      "DoG inner radius"
PXDOG_R2=6       <default 6>      "DoG outer radius"
FLD=Y            <default Y>      "Y=Use fold mask files, N=Set all masks to ones"
SCALE=1          <default 1.0>    "Image-B / image-A scale factor"
XSCALE=1         <default 1.0>    "Image-B / image-A x-axis scale factor"
YSCALE=1         <default 1.0>    "Image-B / image-A y-axis scale factor"
SKEW=0           <default 0.0>    "Transform elem[3] user-applied distortion"
SLOPPY_SL=N      <default N>      "Y=Input transforms are rough"
OLAP1D=100       <default 100>    "Min 1D pix full-size overlap"
OLAP2D_SL=25000  <default 25000>  "Min 2D pix full-size overlap same layer"
OLAP2D_XL=200000 <default 200000> "Min 2D pix full-size overlap cross layer"
HFANG_SL=5       <default 5.0>    "Half angle search range same layer"
HFANG_XL=45      <default 45.0>   "Half angle search range cross layer"
QTRSH_SL=0.08    <default 0.08>   "Q corr thresh same layer"
QTRSH_XL=0.08    <default 0.08>   "Q corr thresh cross layer"
RTRSH_SL=0       <default 0.0>    "R corr thresh same layer"
RTRSH_XL=0.3     <default 0.3>    "R corr thresh cross layer"
TWEAKS=N         <default N>      "Y=Try 8 near unity distortions"
INPALN=N         <default N>      "Y=Input transforms pretty good"
DINPUT=350       <default 350>    "Max translation pixels from input"

*These lines are echoed to reports, so use spaces to neaten!
