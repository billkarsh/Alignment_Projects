Parameters for the (dmesh) detailed mesh pipeline stage.

PXBRO=1     <default 2>        "Max Leg poly order for bkg removal"
PXDOG=N     <default N>        "Y=Apply DoG filter"
PXDOG_R1=3  <default 3>        "DoG inner radius"
PXDOG_R2=6  <default 6>        "DoG outer radius"
FLD=Y       <default Y>        "Y=Use fold mask files, N=Set all masks to ones"
DSL=Y       <default Y(EM)>    "Y=Enable same layer mesh optimizer"
DIT=0.05    <default 0.10>     "Detailed optimizer min starting correlation"
DAF=0.9     <default 0.9>      "Affine-only DFT multiplier"
DFT=0.07    <default 0.305>    "Detailed optimizer min final correlation"
TMC=30      <default 30.0%>    "Optimizer limit of max triangle area change"
TSC=9       <default 9.0%>     "Optimizer limit of sum triangle area change"
MNL=300     <default 300>      "Triangle edges should be between MNL and 2*MNL"
MTA=10000   <default 10000>    "Minimum allowable size of a triangle (pixels)"
MMA=25000   <default 100000>   "This much area should map from one region to another"
ONE=N       <default N>        "Y=Use just one affine transform"
IFM=0.50    <default 0.50>     "Initial Fourier Metric must be this good after affine match"
FFM=0.65    <default 0.65>     "Final Fourier Metric must be this good after deformable mesh"
FYL=0.5     <default 0.50>     "This fraction of pixels must be yellow"
CPD=0.5     <default 0.50>     "Correlation + DFM must be this big"
EMM=Y       <default Y>        "Y=Use the Earth Mover's Metric, N=Use Fourier"
EMT=0.2     <default 0.2>      "Threshold for Earth Movers Metric"
WDI=N       <default N>        "Y=Write the differencing images used for Fourier and EMM metrics"
LDA=0.2     <default 0.2>      "Limit of change in angle (rad) between triangles"
LDR=0.2     <default 0.1>      "Limit of change in sine params between triangles"
LDC=200     <default 100.0>    "Limit of change in XY between triangles"
DXY=200     <default 100.0>    "How far can a transform be from one of the user input XYs"
WMT=Y       <default Y>        "Y=Write map.tif files"
WTT=Y       <default Y>        "Y=Write tf.txt files"

*These lines are echoed to reports, so use spaces to neaten!
