# Parameters for thumbnail and detailed mesh matching pipeline stages.
#
# _SL signifies same-layer, _XL cross-layer
#

PXBRO=1            <default 2>        "Max Legendre poly order for bkg removal"
PXDOG=N            <default N>        "Y=Apply DoG filter"
PXDOG_R1=3         <default 3>        "DoG inner radius"
PXDOG_R2=6         <default 6>        "DoG outer radius"
FLD=Y              <default Y>        "Y=Use fold mask files, N=Set masks to ones"
PRETWEAK=N         <default N>        "Y=Auto-distort before angle scan"
SCALE=1            <default 1.0>      "Image-B / image-A scale factor"
XSCALE=1           <default 1.0>      "Image-B / image-A x-axis scale factor"
YSCALE=1           <default 1.0>      "Image-B / image-A y-axis scale factor"
SKEW=0             <default 0.0>      "Transform elem[3] user-applied distortion"
SLOPPY_SL=N        <default N>        "Y=Input transforms are rough"
OLAP1D_SL=0        <default 0>        "Min 1D pix full-size overlap"
OLAP1D_XL=0        <default 0>        "Min 1D pix full-size overlap"
OLAP2D_SL=25000    <default 25000>    "Min 2D pix full-size overlap"
OLAP2D_XL=200000   <default 200000>   "Min 2D pix full-size overlap"
NBMXHT_SL=0.75     <default 0.75>     "Max F(neib)/F(pk) in guard zone"
NBMXHT_XL=0.75     <default 0.75>     "Max F(neib)/F(pk) in guard zone"
HFANGDN_SL=5       <default 5.0>      "Half angle search range denovo"
HFANGDN_XL=45      <default 45.0>     "Half angle search range denovo"
HFANGPR_SL=1       <default 1.0>      "Half angle search range prior angles"
HFANGPR_XL=1       <default 1.0>      "Half angle search range prior angles"
RTRSH_SL=0.3       <default 0.3>      "R corr thresh"
RTRSH_XL=0.3       <default 0.3>      "R corr thresh"
TWEAKS=N           <default N>        "Y=Try 8 near unity distortions"
INPALN=N           <default N>        "Y=Input transforms pretty good"
DINPUT=350         <default 350>      "Max translation pixels from input"
DSL=Y              <default Y(EM)>    "Y=Enable mesh optimizer"
DIT_SL=0.2         <default 0.20>     "Min mesh starting corr"
DIT_XL=0.2         <default 0.20>     "Min mesh starting corr"
DFA_SL=0.9         <default 0.3>      "Affine-only final corr"
DFA_XL=0.9         <default 0.3>      "Affine-only final corr"
DFT_SL=0.5         <default 0.5>      "Full mesh final corr"
DFT_XL=0.5         <default 0.5>      "Full mesh final corr"
TMC=30             <default 30.0%>    "Max single triangle area change"
TSC=9              <default 9.0%>     "Max summed triangle area change"
MNL=300            <default 300>      "Triangle edges between MNL and 2*MNL"
MTA=10000          <default 10000>    "Min triangle size (pixels)"
MMA=25000          <default 100000>   "Min 2D overlap for mesh"
ONE=N              <default N>        "Y=Use just one affine transform"
IFM=0.50           <default 0.50>     "Min Fourier Metric after affine-only"
FFM=0.65           <default 0.65>     "Min Fourier Metric after full mesh"
FYL=0.5            <default 0.50>     "Min fraction of yellow pixels"
CPD=0.5            <default 0.50>     "Min Correlation + DFM"
EMM=Y              <default Y>        "Y=Use Earth Mover's Metric, N=Use Fourier"
EMT=0.2            <default 0.2>      "Earth Movers Metric thresh"
WDI=N              <default N>        "Y=Write diff images (Fourier, EMM metrics)"
LDA=0.2            <default 0.2>      "Max radian delta between triangles"
LDR=0.2            <default 0.1>      "Max sine delta between triangles"
LDC=200            <default 100.0>    "Max XY delta between triangles"
DXY=200            <default 100.0>    "Max delta from user input XYs"
WMT=Y              <default Y>        "Y=Write map.tif files"
WTT=Y              <default Y>        "Y=Write tf.txt files"

# *These lines are echoed to reports, so use spaces to neaten!
