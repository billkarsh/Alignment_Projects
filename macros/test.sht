#!/bin/sh

# http://tldp.org/LDP/abs/html/internalvariables.html


# Get current directory into a variable
# here="`pwd`"


# Get tokens from a file
# for i in $(cat $1)
# do
# 	echo $i
# done


# Get lines from a file
# IFS="\n"
# for i in $(cat $1)
# do
# 	echo $i
# done
# unset IFS


# Copy file arg1 to arg1_v2
# ext=$(echo $1 | sed 's|[^.]*\(.*\)|\1|')
# base=$(echo $1 | sed 's|\(.*\)'$ext'|\1|')
# v2=$base"_v2"$ext
# echo $v2


# find files at all depths from here like *.sht
# find . -name "*.sht"

# Rename all files containing '.sh' to '.sht'
# rename <from pattern <to pattern> <where>
# rename .sh .sht *.sh


# Get path part of filename from $1
# dir=$(dirname $1)


# Set env var MRC_TRIM to 12 for this script
# export MRC_TRIM=12


# if ((2 + 3 == 5))
# then
# echo yes
# else
# echo no
# fi


if [ "$1" != "" ]
then
echo is arg
else
echo no arg
fi

